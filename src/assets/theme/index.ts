import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ff4500",
    },
    secondary: {
      main: "#ffffff",
    },
    text: {
      primary: "#ff4500",
      secondary: "#ffd700",
    },
  },
});
